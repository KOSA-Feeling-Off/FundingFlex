<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.fundingflex.mybatis.mapper.notification.NotificationMapper">

	<!-- 알림 저장 -->
	<insert id="insertNotification">
		INSERT INTO notification (fc_id, user_id, message)
		  VALUES (#{fcId}, #{userId}, #{message})
	</insert>


	<!-- 알림 조회 (userId 기준 최근 5개) -->
	<select id="findByUserId">
		SELECT noti_id
			 , fc_id
			 , user_id
			 , message
			 , created_at
			 , is_read
			 , read_at
		FROM notification
	   WHERE user_id = #{userId}
    ORDER BY created_at 	DESC
       FETCH FIRST 5 ROWS ONLY
	</select>


	<!-- 읽음 업데이트 -->
	<update id="updateNotificationReadStatus" parameterType="map">
		UPDATE notification
		   SET is_read = 'Y', read_at = CURRENT_TIMESTAMP
		 WHERE user_id = #{userId}
	</update>

</mapper>